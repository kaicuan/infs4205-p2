# Fill in database credentials
POSTGRES_HOST=
POSTGRES_PORT=
POSTGRES_DATABASE=
POSTGRES_USER=
POSTGRES_PASSWORD=

# In macOS, AUTH_SECRET can be generated by running: `openssl rand -base64 32`
# Alternatively, can generate via https://generate-secret.vercel.app/32
AUTH_SECRET=
AUTH_TRUST_HOST=true